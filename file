pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            git credentialsId: 'sree', url: 'https://github.com/srinivasareddy4218/loginApp.git'
         }
      }
      stage('testing') {
         steps {
            bat label: '', script: 'cd loginApp && mvn clean package'
         }
      }
        stage('Deploy') {
         steps {
             bat label: '', script: 'cp login.war C:\\Users\\sarikatla\\Downloads\\tomcat\\tomcat\\webapps'

         }
      }
   }
}
